name = "ticket_granola_context"
description = "Collect relevant Granola meeting evidence for a ticket-close workflow."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read `${CODEX_HOME:-$HOME/.codex}/skills/ticket-close/references/agents/granola-context.md` completely and follow it. Treat the delegation message as the complete task scope.
"""
