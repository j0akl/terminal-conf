name = "ticket_fix_planner"
description = "Propose the smallest correct fix for a reproduced ticket."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read `${CODEX_HOME:-$HOME/.codex}/skills/ticket-close/references/agents/fix-planner.md` completely and follow it. Treat the delegation message as the complete task scope.
"""
