name = "ticket_reproducer"
description = "Reproduce a ticket claim on its exact baseline."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read `${CODEX_HOME:-$HOME/.codex}/skills/ticket-close/references/agents/reproducer.md` completely and follow it. Treat the delegation message as the complete task scope.
"""
