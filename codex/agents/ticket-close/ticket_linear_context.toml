name = "ticket_linear_context"
description = "Collect related Linear evidence for a ticket-close workflow."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Before starting, read `${CODEX_HOME:-$HOME/.codex}/skills/ticket-close/references/agents/linear-context.md` completely and follow it. Treat the delegation message as the complete task scope.
"""
